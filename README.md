# 🚚 Shipment App — Система расчета доставки автомобилей

Веб-приложение для расчета стоимости и сроков доставки автомобилей между городами.

## 🏗️ Архитектура

- **Backend:** Python 3.12 + FastAPI + SQLAlchemy + PostgreSQL
- **Frontend Client:** React + TypeScript + Vite + Tailwind CSS
- **Frontend Admin:** React + TypeScript + Vite + Tailwind CSS
- **База данных:** PostgreSQL 16
- **Контейнеризация:** Docker + Docker Compose

## 🚀 Быстрый старт (Docker)

### Предварительные требования
- Docker >= 20.10
- Docker Compose >= 2.0

### Запуск проекта

1. **Клонировать репозиторий:**
```bash
git clone <repository-url>
cd shipment-app
```

2. **Запустить все сервисы:**
```bash
docker compose up --build
```

Первый запуск займет несколько минут (сборка образов, установка зависимостей).

3. **Доступ к приложениям:**
   - **Client (пользовательский интерфейс):** http://localhost:5173
   - **Admin (админ-панель):** http://localhost:5174
   - **API (Swagger документация):** http://localhost:8000/docs
   - **PostgreSQL:** localhost:5432

### Логины и пароли по умолчанию

**Админ-панель:**
- Логин: `admin`
- Пароль: `admin123`

**База данных:**
- User: `postgres`
- Password: `postgres`
- Database: `shipment`

---

## 📂 Структура проекта

```
shipment-app/
├── backend/              # FastAPI бэкенд
│   ├── app/
│   │   ├── api/v1/      # REST API endpoints
│   │   ├── repositories/ # Модели и репозитории
│   │   ├── services/    # Бизнес-логика
│   │   ├── schemas/     # Pydantic DTO
│   │   └── infra/       # Конфигурация, БД, seed
│   ├── alembic/         # Миграции (автогенерируются)
│   ├── requirements.txt
│   └── Dockerfile
├── frontend-client/      # Клиентский интерфейс
│   └── src/
├── frontend-admin/       # Админ-панель
│   └── src/
├── docker-compose.yml
└── README.md
```

---

## 🛠️ Разработка (локально без Docker)

### Backend

1. **Установка зависимостей:**
```bash
cd backend
python3.12 -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
```

2. **Запуск БД:**
```bash
docker compose up -d db
```

3. **Запуск сервера:**
```bash
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/shipment \
uvicorn app.main:app --reload --port 8000
```

### Frontend Client

```bash
cd frontend-client
npm install
npm run dev
```

### Frontend Admin

```bash
cd frontend-admin
npm install
npm run dev
```

---

## 📊 База данных

### Автоматическое создание таблиц

При первом запуске `seed.py` автоматически:
- Создает все таблицы
- Добавляет начальные города (Москва, Сочи, Санкт-Петербург, Бишкек) с координатами
- Заполняет тарифы на 12 месяцев (по умолчанию: 150 руб/км ≤1000 км, 100 руб/км >1000 км)
- Создает фикс-маршруты (Москва-Сочи: 200 000 руб, Москва-Бишкек: 350 000 руб)
- Создает админа (login: `admin`, password: `admin123`)
- Импортирует расстояния из offline_matrix.json в таблицу city_distances

### Миграции (опционально)

Если требуется использовать Alembic для миграций:

```bash
# Создать миграцию
cd backend
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/shipment \
venv/bin/alembic revision --autogenerate -m "migration name"

# Применить миграции
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/shipment \
venv/bin/alembic upgrade head
```

---

## 📖 Документация

1. **Swagger API:** http://localhost:8000/docs — интерактивная документация всех endpoints
2. **ReDoc API:** http://localhost:8000/redoc — альтернативная документация
3. **Схема БД:** см. `DATABASE_SCHEMA.md`
4. **User Stories:** см. `USER_STORIES.md`

---

## 🧪 Тестирование

```bash
cd backend
pytest app/tests/ -v --cov=app
```

---

## 🔒 Безопасность

- Пароли хешируются через bcrypt
- JWT токены для аутентификации админов
- CORS настроен (в продакшне ограничить домены)

---

## 📝 Makefile команды

```bash
make up                  # Запустить все в Docker
make db-up               # Запустить только БД
make api-dev             # Запустить backend локально
make client-dev          # Запустить client локально
make admin-dev           # Запустить admin локально
make install-all         # Установить все зависимости
```

---

## 🐛 Возможные проблемы

### Порты заняты
Если порты 5173, 5174, 8000 или 5432 заняты, измените в `docker-compose.yml`:
```yaml
ports:
  - "НОВЫЙ_ПОРТ:СТАРЫЙ_ПОРТ"
```

### База данных не создается
Очистить volumes и пересобрать:
```bash
docker compose down -v
docker compose up --build
```

### Frontend не подключается к API
Проверьте переменную `VITE_API_URL` в `docker-compose.yml` (должна быть `http://localhost:8000`).

---

## 📦 Production Deployment

1. **Переменные среды:**
   - `DATABASE_URL` — строка подключения к PostgreSQL
   - `VITE_API_URL` — URL бэкенда для фронтенда
   - `API_DEBUG` — установить в `false` для продакшна

2. **CORS:**
   Обновить `app/main.py` — ограничить `allow_origins` конкретными доменами.

3. **Nginx:**
   Настроить reverse proxy для API и фронтендов.

4. **SSL:**
   Использовать Let's Encrypt для HTTPS.

---

## 🎯 Основные возможности

### Клиентская часть
- Создание заказа на доставку автомобиля
- Автоматический расчет стоимости и сроков
- Просмотр списка заказов по номеру телефона
- Оплата заказа (имитация)

### Админ-панель
- Управление заказами (просмотр, изменение статуса, удаление)
- Управление городами (добавление, редактирование, координаты)
- Управление тарифами (настройка цен для каждого месяца)
- Управление расстояниями между городами (ручной ввод приоритетнее авто-расчета)

### API
- REST API с OpenAPI (Swagger) документацией
- Автоматическая валидация через Pydantic
- Обработка ошибок с понятными сообщениями
- JWT аутентификация для админов

---

## 📄 Лицензия

Proprietary — внутренний проект.

